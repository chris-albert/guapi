{{#if isTabs}}
  {{#tab-component model=content.tabs}}
    {{outlet}}
  {{/tab-component}}
{{else if isForm}}
  {{#generic-form
    model=content.form
    queryParams=queryParams
    route=route
  }}{{/generic-form}}
{{/if}}