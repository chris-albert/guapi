{{#power-select-multiple
  options=names
  renderInPlace=true
  selected=selected
  onchange=(action "onChange")
  onkeydown=(action "handleKeydown")
  oninput=(action "onInput")
  searchField="display"
  as |option select|}}
  {{option.display}}
{{/power-select-multiple}}
